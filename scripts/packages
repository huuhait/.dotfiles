#!/bin/bash

dependencies=$(sed 's/.*/&/;$!s/$/ /' ./scripts/dependencies.txt | tr -d '\n')
remove_dependencies=$(sed 's/.*/&/;$!s/$/ /' ./scripts/remove_dependencies.txt | tr -d '\n')

sudo pacman -Syu
yay -S paru ansible
paru -S $dependencies
paru -R $remove_dependencies
